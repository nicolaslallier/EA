# Le routeur est en mode `history` (docs/adr/0008) : /analyse/impact n'est pas
# un fichier, et un rechargement de page doit recevoir index.html.
server {
    listen 80;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
